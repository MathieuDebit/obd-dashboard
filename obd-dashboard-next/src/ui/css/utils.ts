/**
 * @file CSS utility values shared by UI primitives.
 */
/**
 * CSS selectors for light/dark themes used when injecting theme-specific rules.
 */
export const THEMES = { light: "", dark: ".dark" }
